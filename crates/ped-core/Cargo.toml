[package]
name = "ped-core"
version.workspace = true
edition.workspace = true

[dependencies]
serde.workspace = true
serde_json.workspace = true

# rust-sci-core crates — the actual computation engine
sci-units.workspace = true       # Unit conversions (kg↔g, age conversions, temp scales)
sci-clinical.workspace = true    # Clinical calculations, vital sign ranges, scoring systems
sci-growth.workspace = true      # Growth chart percentiles, z-scores, WHO/Fenton curves
