// Stub WASM module — replaced by actual wasm-pack output after `npm run build:wasm`
export default function init() {
  console.warn('WASM module not built. Run `npm run build:wasm` to enable drug dose calculations.');
  return Promise.resolve();
}
